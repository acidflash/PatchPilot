# Copy this file to deploy.env and fill in your values.
# deploy.env is gitignored and never committed.

# SSH hostname or IP of your server
DEPLOY_HOST=192.168.1.100

# Path to PatchPilot on the server
DEPLOY_DIR=/home/youruser/patchpilot
